<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>¡Bienvenido a la familia CablePlus!</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        :root {
            --verde: #4CAF50;
            --azul: #2196F3;
            --naranja: #FF9800;
            --degradado: linear-gradient(135deg, #4CAF50 0%, #2196F3 100%);
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: #f8f9fa;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-image: url('https://images.unsplash.com/photo-1551434678-e076c223a692?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }
        
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: 0;
        }
        
        .contenedor-gracias {
            position: relative;
            z-index: 1;
            background: rgba(255, 255, 255, 0.95);
            padding: 50px;
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
            max-width: 700px;
            width: 90%;
            text-align: center;
            animation: aparecer 0.8s ease-out;
            transform-style: preserve-3d;
            perspective: 1000px;
        }
        
        @keyframes aparecer {
            from { 
                opacity: 0;
                transform: translateY(30px) rotateX(20deg);
            }
            to { 
                opacity: 1;
                transform: translateY(0) rotateX(0);
            }
        }
        
        .icono-gracias {
            font-size: 5rem;
            color: var(--verde);
            margin-bottom: 20px;
            animation: latido 1.5s infinite;
        }
        
        @keyframes latido {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        h1 {
            color: var(--azul);
            font-size: 2.5rem;
            margin-bottom: 20px;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.1);
        }
        
        p {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 15px;
        }
        
        .destacado {
            font-weight: bold;
            color: var(--azul);
            font-size: 1.3rem;
        }
        
        .familia {
            display: inline-block;
            background: var(--degradado);
            color: white;
            padding: 8px 20px;
            border-radius: 50px;
            margin: 15px 0;
            font-weight: bold;
            box-shadow: 0 4px 15px rgba(33, 150, 243, 0.3);
        }
        
        .btn-volver {
            display: inline-block;
            background: var(--degradado);
            color: white;
            padding: 15px 40px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            margin-top: 30px;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-size: 1.1rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            border: none;
            cursor: pointer;
        }
        
        .btn-volver:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            background: linear-gradient(135deg, #3d8b40 0%, #0d8aee 100%);
        }
        
        .confetti {
            position: absolute;
            width: 10px;
            height: 10px;
            background-color: var(--azul);
            opacity: 0;
            z-index: 10;
            animation: confetti 5s ease-in-out infinite;
        }
        
        @keyframes confetti {
            0% {
                transform: translateY(0) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(500px) rotate(720deg);
                opacity: 0;
            }
        }
        
        @media (max-width: 768px) {
            .contenedor-gracias {
                padding: 30px 20px;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            p {
                font-size: 1.1rem;
            }
            
            .icono-gracias {
                font-size: 4rem;
            }
        }
    </style>
</head>
<body>
    <div class="overlay"></div>
    
    <div class="contenedor-gracias">
        <div class="icono-gracias">
            <i class="fas fa-heart"></i>
        </div>
        
        <h1>¡Bienvenido a la familia CablePlus!</h1>
        
        <div class="familia">
            #FamiliaCablePlus
        </div>
        
        <p>¡Estamos emocionados de tenerte con nosotros!</p>
        
        <p class="destacado">Tu solicitud ha sido recibida con éxito y estamos trabajando en ella.</p>
        
        <p>En menos de 24 horas recibirás una llamada de nuestro equipo para coordinar todos los detalles y darte la mejor bienvenida a nuestra comunidad.</p>
        
        <p>Mientras tanto, si tienes alguna pregunta urgente, no dudes en contactarnos al <strong>2716-6000</strong> o por WhatsApp al <strong>7275-6074</strong>.</p>
        
        <p>¡Prepárate para disfrutar de la mejor conexión y entretenimiento!</p>
        
        <a href="index.html" class="btn-volver">
            <i class="fas fa-home"></i> Volver al inicio
        </a>
    </div>
    
    <script>
        // Crear confeti
        function crearConfetti() {
            const colores = ['#4CAF50', '#2196F3', '#FF9800', '#9C27B0', '#E91E63'];
            
            for (let i = 0; i < 50; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.backgroundColor = colores[Math.floor(Math.random() * colores.length)];
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.top = -10 + 'px';
                confetti.style.width = Math.random() * 10 + 5 + 'px';
                confetti.style.height = Math.random() * 10 + 5 + 'px';
                confetti.style.animationDelay = Math.random() * 5 + 's';
                confetti.style.animationDuration = Math.random() * 3 + 3 + 's';
                document.body.appendChild(confetti);
            }
        }
        
        // Ejecutar cuando se cargue la página
        window.onload = function() {
            crearConfetti();
            
            // Mostrar mensaje especial si viene del formulario
            const urlParams = new URLSearchParams(window.location.search);
            if(urlParams.has('success')) {
                // Animación adicional
                const icono = document.querySelector('.icono-gracias');
                icono.style.animation = 'latido 1.5s infinite, giro 2s 1';
            }
        };
    </script>
</body>
</html>